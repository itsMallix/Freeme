import 'package:flutter/material.dart';

class BuildCardObjectiveProfile extends StatelessWidget {
  const BuildCardObjectiveProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
