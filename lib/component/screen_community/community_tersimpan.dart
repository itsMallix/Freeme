import 'package:flutter/material.dart';

class CommunityTersimpan extends StatelessWidget {
  const CommunityTersimpan({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
