import 'package:flutter/material.dart';

class ScreenCommunityMain extends StatelessWidget {
  const ScreenCommunityMain({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Community"),
    );
    ;
  }
}
