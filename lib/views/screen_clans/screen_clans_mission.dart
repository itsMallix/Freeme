import 'package:flutter/material.dart';

class ScreenClansMission extends StatelessWidget {
  const ScreenClansMission({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
