import 'package:flutter/material.dart';

class ScreenClansCreate extends StatelessWidget {
  const ScreenClansCreate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
